program programa1;

var acum, total : int;

void func_1(x : float, y : float)[
    var z : float;
    {
        valor_x = x + 10;
        valor_y = y * 20;
        
        if (valor_x / 2 < valor_y * 2) {
            total = total + 1;
        } else {
            total = 0;
        };
        
        print(total);
    }
];

main {
    acum = 0;
    
    do { 
        func_1(x, y); 
    } while ( a > 3 );
    
    func_1(x, y);
    
    print("Fin");
} end
