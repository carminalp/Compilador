program prueba; 
    var exam1, exam2, exam3: float;
    void avg(exam4: float, exam5: float, exam6: int)
    [
        var resultado : float;
        {
            resultado = (exam4 + exam5 + exam6)/3;
        }
    ];

    void alpha(exw: float, exq: float, exr: int)
    [
        {
            if(exq > exw){
                print(exr);

            };
        }
    ]; 

    void resta(f1: float, f2: float, f3: float)
    [
        var menos : float;
        {
            menos = f1 + f2 - f3;
            do {
                exam2 = exam3 - 1;
                print("Lalo");
            } while (f1 > f3+f2);
        }
    ];

    main {
        alpha(3.3, 2.1, 2);
        avg(98.3, -96.2, -89);
        exam2 = 4;
    }
end